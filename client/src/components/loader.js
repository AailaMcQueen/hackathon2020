import React from 'react'
import ReactLoading from "react-loading"

const Loader = () => (
    <ReactLoading className="loader" type="spin" color="#000000" height={'5%'} width={'5%'} />
)

export default Loader